extern crate gl;
extern crate nalgebra_glm as glm;
extern crate sdl2;

pub fn getProjectionMatrix() {}

pub fn computeMatricesFromInputs() {}

pub fn getViewMatrix() {

    // let direction = glm::vec3(
    //     f32::cos(verticalAngle) * f32::sin(horizontalAngle),
    //     f32::sin(verticalAngle),
    //     f32::cos(verticalAngle) * f32::cos(horizontalAngle)
    // );

    // let View = glm::look_at(
    //     &glm::vec3(4.0, 3.0, 3.0), // Camera is at (4,3,3), in World Space
    //     &direction, // and looks at the origin
    //     &glm::vec3(0.0, 1.0, 0.0), // Head is up (set to 0,-1,0 to look upside-down)
    // );

    // return direction
}

pub fn test1() {

    // let mut xpos;
    // let mut ypos;
    // xpos = sdl2::mouse::MouseState::x();
    // ypos = sdl2::mouse::MouseState::y();
    // println!("x", xpos);
    // println!("y", ypos);
}
